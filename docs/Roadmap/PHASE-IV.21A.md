# Phase IV.21A — The Cartographer's Grid

The Dungeon Master can visually align the rules grid with uploaded battlemat artwork. Square size, X/Y offset, opacity and visibility preview immediately, with one-pixel nudge controls for fine alignment. Saving is authoritative and persists on the Scene. Existing Scenes default to zero offset, 13% opacity and a visible grid. Token normalised coordinates remain independent of calibration.
